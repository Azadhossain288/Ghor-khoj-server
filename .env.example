# MongoDB Atlas connection string
MONGODB_URI=
# Better Auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
# Google OAuth (from Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend URL (for CORS + auth redirects)
FRONTEND_URL=http://localhost:3000

# AI Provider (Groq is fast + free tier; OpenAI-compatible API shape)
GROQ_API_KEY=
AI_MODEL=

# Demo account (used by seed script + demo login button)
DEMO_USER_EMAIL=demo@ghorkhoj.com
DEMO_USER_PASSWORD=Demo@1234

PORT=5000
